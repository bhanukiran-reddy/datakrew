/**
 * GraphQL schema types — generated by GraphQL Code Generator.
 * Regenerate with: npm run codegen
 * (Set NEXT_PUBLIC_GRAPHQL_ENDPOINT to introspect live CMS, or uses lib/graphql/schema.graphql)
 */

export type Maybe<T> = T | null;

export type MediaImage = {
  url: string;
  alt: string;
  width: number;
  height: number;
};

export type SEO = {
  title: string;
  description: string;
  canonicalUrl?: Maybe<string>;
  ogImage?: Maybe<MediaImage>;
  noIndex?: Maybe<boolean>;
  noFollow?: Maybe<boolean>;
};

export type Section = {
  id: string;
  type: string;
  data?: Maybe<Record<string, unknown>>;
};

export type Page = {
  id: string;
  title: string;
  slug: string;
  locale: string;
  pageTemplate?: Maybe<string>;
  seo: SEO;
  sections: Array<Section>;
};

export type NavItem = {
  id: string;
  label: string;
  href: string;
  children?: Maybe<Array<NavItem>>;
};

export type Navigation = {
  header: Array<NavItem>;
  footer: Array<NavItem>;
};

export type Redirect = {
  from: string;
  to: string;
  statusCode: number;
  locale?: Maybe<string>;
};

export type LocaleInfo = {
  code: string;
  name: string;
  isDefault: boolean;
};

export type SiteSettings = {
  siteName: string;
  logo?: Maybe<MediaImage>;
  favicon?: Maybe<MediaImage>;
  defaultSeo: SEO;
};
